import{getSupabase}from"./supabase.js";const db=await getSupabase(),app=document.querySelector("#app");const esc=s=>String(s??"").replace(/[&<>"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[m]));async function run(){const u=await db.auth.getUser();if(!u.data.user){app.innerHTML='<section class="panel card">Sign in to view your learning profile.</section>';return}const q=await db.from("podcast_creator_learning_profiles").select("*").eq("user_id",u.data.user.id).maybeSingle();if(q.error)throw q.error;if(!q.data){app.innerHTML='<section class="panel card"><h2>Your learning loop is starting</h2><p>Complete experiments to build an evidence history.</p></section>';return}const x=q.data;app.innerHTML='<div class="grid"><section class="panel card"><div class="muted">Experiments</div><div class="metric">'+x.experiments_count+'</div></section><section class="panel card"><div class="muted">Positive observations</div><div class="metric">'+(x.positive_sessions+x.positive_listening+x.positive_followers)+'</div></section><section class="panel card"><div class="muted">Negative observations</div><div class="metric">'+(x.negative_sessions+x.negative_listening+x.negative_followers)+'</div></section></div><section class="panel card" style="margin-top:18px"><h2>Evidence Patterns</h2><div class="history">'+(x.patterns||[]).map(p=>'<div><span class="status">'+esc(p.status)+'</span> <b>'+esc(p.metric.replaceAll("_"," "))+'</b><p>'+p.positive+' positive · '+p.negative+' negative · '+esc(p.confidence)+' evidence</p></div>').join("")+'</div><p class="muted">Status describes the amount and direction of observed evidence. It does not indicate causation.</p></section><section class="panel card" style="margin-top:18px"><h2>Next Experiments to Consider</h2><ul>'+(x.recommendations||[]).map(r=>'<li>'+esc(r)+'</li>').join("")+'</ul></section><section class="panel card" style="margin-top:18px"><h2>Learning Timeline</h2><div class="history">'+(x.timeline||[]).map(h=>'<div><b>'+esc(h.name)+'</b><br><small>'+new Date(h.completed_at).toLocaleDateString()+'</small><p>'+Object.entries(h.deltas||{}).map(([k,v])=>esc(k)+': '+(Number(v)>=0?"+":"")+v).join(" · ")+'</p></div>').join("")+'</div></section>'}run().catch(e=>app.textContent=e.message);