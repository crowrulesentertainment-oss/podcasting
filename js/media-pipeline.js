import {getSupabase} from "./supabase.js";
export async function uploadCreatorAsset(projectId,file,assetType="other",episodeId=null){const s=await getSupabase();if(!s)throw new Error("Supabase unavailable");const {data:{user}}=await s.auth.getUser();if(!user)throw new Error("Sign in required");const ext=(file.name.split(".").pop()||"bin").toLowerCase();const path=user.id+"/"+projectId+"/"+(episodeId||"library")+"/"+Date.now()+"."+ext;const bucket=assetType==="artwork"||assetType==="thumbnail"?"cr-podcast-artwork":"cr-podcast-media";const u=await s.storage.from(bucket).upload(path,file,{cacheControl:"3600",upsert:false,contentType:file.type||undefined});if(u.error)throw u.error;const r=await s.from("cr_creator_media_assets_62").insert({owner_id:user.id,project_id:projectId,episode_id:episodeId,asset_type:assetType,bucket_id:bucket,storage_path:path,original_filename:file.name,mime_type:file.type,size_bytes:file.size,processing_status:"uploaded"}).select().single();if(r.error)throw r.error;return r.data;}
export async function listCreatorAssets(projectId,episodeId=null){const s=await getSupabase();if(!s)return [];let q=s.from("cr_creator_media_assets_62").select("*").eq("project_id",projectId).order("created_at",{ascending:false});if(episodeId)q=q.eq("episode_id",episodeId);const r=await q;if(r.error)throw r.error;return r.data||[];}
export async function creatorAssetUrl(asset){const s=await getSupabase();if(asset.bucket_id==="cr-podcast-artwork")return s.storage.from(asset.bucket_id).getPublicUrl(asset.storage_path).data.publicUrl;const r=await s.storage.from(asset.bucket_id).createSignedUrl(asset.storage_path,3600);if(r.error)throw r.error;return r.data.signedUrl;}