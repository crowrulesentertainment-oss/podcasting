import{getSupabase}from'./supabase.js';const db=await getSupabase(),root=document.querySelector('#history');const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));async function init(){const u=(await db.auth.getUser()).data.user;if(!u){root.innerHTML='<div class="panel empty"><p>Sign in required.</p></div>';return}const r=await db.from('podcast_evidence_health_transition_summaries').select('*').eq('user_id',u.id).order('period_end',{ascending:false}).limit(100);if(r.error)throw r.error;const rows=r.data||[];if(!rows.length){root.innerHTML='<div class="panel empty"><p>No health summaries have been recorded yet.</p></div>';return}root.innerHTML=rows.map(x=>'<article class="panel box" style="margin-bottom:14px"><div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap"><div><p class="eyebrow">'+esc(x.confidence_level||'descriptive')+' • '+esc(x.anomaly_state||'normal')+'</p><h2>'+esc(x.summary||'Health summary')+'</h2></div><strong>'+esc(x.review_status||'unreviewed')+'</strong></div><p class="muted">'+new Date(x.period_start).toLocaleDateString()+' – '+new Date(x.period_end).toLocaleDateString()+' • '+esc(x.sample_size)+' transitions • score '+esc(Number(x.baseline_score??0).toFixed(1))+' → '+esc(Number(x.ending_score??0).toFixed(1))+'</p><p>'+esc((x.anomaly_reasons||[]).join(' ')||'No anomaly reasons recorded.')+'</p><p class="muted">'+esc((x.review_note||'No creator review note recorded.'))+'</p></article>').join('')}init().catch(e=>root.innerHTML='<div class="panel empty"><p>'+esc(e.message)+'</p></div>');